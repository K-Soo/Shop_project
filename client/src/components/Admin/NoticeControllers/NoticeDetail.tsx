import React from "react";
import styled from "styled-components";

interface INoticeDetail {
  
}

const S = {
  NoticeDetail: styled.div`
  `,
}

export default function NoticeDetail({}:INoticeDetail) {
  return (
    <S.NoticeDetail>
NoticeDetail
    </S.NoticeDetail>
  );
}
