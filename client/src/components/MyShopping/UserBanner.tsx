import React from "react";
import styled from "styled-components";

interface IUserBanner {
  
}

const S = {
  UserBanner: styled.div`
  `,
}

export default function UserBanner({}:IUserBanner) {
  return (
    <S.UserBanner>
UserBanner
    </S.UserBanner>
  );
}
