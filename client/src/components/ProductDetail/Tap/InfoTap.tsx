import React from "react";
import styled from "styled-components";

interface IInfoTap {
  
}

const S = {
  InfoTap: styled.div`
  `,
}

export default function InfoTap({}:IInfoTap) {
  return (
    <S.InfoTap>
      InfoTap
    </S.InfoTap>
  );
}
