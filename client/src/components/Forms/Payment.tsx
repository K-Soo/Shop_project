import React from "react";
import styled from "styled-components";

interface IPayment {
  
}

const S = {
  Payment: styled.div`
  `,
}

export default function Payment({}:IPayment) {
  return (
    <S.Payment>
      Payment
    </S.Payment>
  );
}
